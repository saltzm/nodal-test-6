/// ToyDB CLI entry point.
/// (Full REPL to be implemented in a later step.)

fn main() {
    println!("ToyDB — a toy relational database");
    println!("REPL not yet implemented. Run `cargo test` to verify core types.");
}
